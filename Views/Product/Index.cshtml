@model ProductListViewModel
@{
    ViewData["Title"] = "Sản phẩm";
}

<div class="container">

    <nav aria-label="breadcrumb">
        <ol class="breadcrumb small">
            <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Trang chủ</a></li>
            <li class="breadcrumb-item active" aria-current="page">Sản phẩm</li>
        </ol>
    </nav>

    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-3">
        <h1 class="section-title">Tất cả sản phẩm</h1>
        @if (Model.TotalCount > 0)
        {
            <span class="text-muted small">@Model.TotalCount sản phẩm</span>
        }
    </div>

    @* Vùng bộ lọc / sắp xếp / phân trang (CUS-07) sẽ được thêm ngay tại đây. *@

    @if (Model.TotalCount > 0)
    {
        <div class="row row-cols-2 row-cols-md-3 row-cols-lg-4 g-3">
            @foreach (var product in Model.Products)
            {
                <div class="col">
                    <partial name="_ProductCard" model="product" />
                </div>
            }
        </div>
    }
    else
    {
        <div class="empty-state">
            <div class="empty-icon">📦</div>
            <h2 class="h5 text-body">Chưa có sản phẩm nào</h2>
            <p class="mb-3">Danh sách sẽ hiển thị ngay khi cửa hàng thêm sản phẩm vào database.</p>
            <a class="btn btn-outline-primary btn-sm" asp-controller="Home" asp-action="Index">Về trang chủ</a>
        </div>
    }

</div>
